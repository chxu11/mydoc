---
id: 1
title: 1.JQuery介绍
sidebar_position: 1
---
# 阶段1：React 是什么

## 🎯 学习目标
理解 jquery 的本质和优势,为后续学习打下认知基础。

jQuery 定位语法（选择器）速查表

jQuery 使用 CSS 选择器语法来定位 HTML 元素。以下是常用的 jQuery 定位（选择）语法，适用于 jQuery 1.x、2.x、3.x 及以上版本。

一、基本选择器

语法 描述 示例
------ ------ ------
$("element") 标签选择器：选取所有指定标签的元素 $("p") 选取所有 <p> 元素
$("#id") ID 选择器：选取具有指定 id 的元素 $("#header") 选取 id 为 header 的元素
$(".class") 类选择器：选取所有具有指定 class 的元素 $(".highlight") 选取所有 class 包含 highlight 的元素
$("") 通配符选择器：选取文档中所有元素 $("") 选取全部元素

二、层级选择器

语法 描述 示例
------ ------ ------
$("parent child") 后代选择器：选取 parent 内所有 child 元素（不限层级） $("div p") 选取 div 内所有 p 元素
$("parent > child") 子元素选择器：仅选取 parent 的直接子元素 child $("ul > li") 选取 ul 的直接子 li
$("prev + next") 相邻兄弟选择器：选取紧接在 prev 后的 next 元素 $("h1 + p") 选取紧跟在 h1 后的第一个 p
$("prev ~ siblings") 一般兄弟选择器：选取 prev 后所有同级的 siblings 元素 $("h1 ~ p") 选取 h1 后所有同级的 p 元素

三、基本过滤选择器

语法 描述 示例
------ ------ ------
$("selector:first") 选取第一个匹配元素 $("li:first")
$("selector:last") 选取最后一个匹配元素 $("li:last")
$("selector:even") 选取索引为偶数的元素（从 0 开始） $("tr:even")
$("selector:odd") 选取索引为奇数的元素 $("tr:odd")
$("selector:eq(index)") 选取索引等于 index 的元素 $("li:eq(2)") 第3个 li
$("selector:gt(index)") 选取索引大于 index 的元素 $("li:gt(2)")
$("selector:lt(index)") 选取索引小于 index 的元素 $("li:lt(3)")
$("selector:not(selector)") 排除匹配 selector 的元素 $("input:not(:checked)")

四、内容过滤选择器

语法 描述 示例
------ ------ ------
$("selector:contains('text')") 选取包含指定文本的元素 $("p:contains('Hello')")
$("selector:empty") 选取没有子元素或文本的空元素 $("div:empty")
$("selector:has(selector)") 选取包含特定后代的元素 $("div:has(p)") 包含 p 的 div
$("selector:parent") 选取含有子元素或文本的元素（非空） $("td:parent")

五、可见性过滤选择器

语法 描述 示例
------ ------ ------
$("selector:hidden") 选取所有不可见元素（包括 display:none、type="hidden" 等） $("input:hidden")
$("selector:visible") 选取所有可见元素 $("div:visible")

六、属性选择器

语法 描述 示例
------ ------ ------
$("[attr]") 选取拥有 attr 属性的元素 $("[title]")
$("[attr='value']") 选取 attr 属性值等于 value 的元素 $("[type='text']")
$("[attr!='value']") 选取 attr 属性值不等于 value 的元素 $("[type!='submit']")
$("[attr^='value']") 选取 attr 属性值以 value 开头的元素 $("[href^='https']")
$("[attr$='value']") 选取 attr 属性值以 value 结尾的元素 $("[src$='.jpg']")
$("[attr='value']") 选取 attr 属性值包含 value 的元素 $("[class='btn']")
$("[attr~='value']") 选取 attr 属性值包含完整单词 value 的元素（空格分隔） $("[class~='active']")
$("[attr ='value']") 选取 attr 属性值等于 value 或以 value- 开头的元素 $("[lang ='en']")

七、表单选择器

语法 描述 示例
------ ------ ------
$(":input") 选取所有 input、textarea、select、button 元素 $(":input")
$(":text") 选取所有 type="text" 的 input $(":text")
$(":password") 选取密码框 $(":password")
$(":radio") 选取单选按钮 $(":radio")
$(":checkbox") 选取复选框 $(":checkbox")
$(":submit") 选取提交按钮 $(":submit")
$(":image") 选取图像提交按钮 $(":image")
$(":reset") 选取重置按钮 $(":reset")
$(":button") 选取 button 元素和 type="button" 的 input $(":button")
$(":file") 选取文件上传域 $(":file")
$(":hidden") 选取隐藏域 $(":hidden")
表单对象属性过滤器

语法 描述 示例
------ ------ ------
$(":enabled") 选取所有可用元素 $(":enabled")
$(":disabled") 选取所有禁用元素 $(":disabled")
$(":checked") 选取所有被选中的 radio 或 checkbox $(":checked")
$(":selected") 选取所有被选中的 option 元素 $("option:selected")

💡 提示：
所有选择器均可组合使用，例如：$("div.highlight:visible")
jQuery 选择器返回的是 jQuery 对象，不是原生 DOM 元素。如需获取原生元素，可使用 [index] 或 .get(index)，例如：$("p")[0] 或 $("p").get(0)

✅ 掌握这些选择器，即可高效地在页面中定位和操作任意元素！
